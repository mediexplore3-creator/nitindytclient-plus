"use strict";
function toggleMenu() {
    document.querySelector(".nav__mobile").classList.toggle("hidden"),
        document.querySelector(".nav__mobile-background").classList.toggle("hidden"),
        document.querySelector(".nav__menu--open").classList.toggle("hidden"),
        document.querySelector(".nav__menu--close").classList.toggle("hidden"),
        document.querySelector("html").classList.toggle("mobile-menu-open")
}
function toggleMenu() {
    document.querySelector(".nav__mobile").classList.toggle("hidden"),
        document.querySelector(".nav__mobile-background").classList.toggle("hidden"),
        document.querySelector(".nav__menu--open").classList.toggle("hidden"),
        document.querySelector(".nav__menu--close").classList.toggle("hidden"),
        document.querySelector("html").classList.toggle("mobile-menu-open")
}
function toggleDropdown(e) {
    e.querySelector(".dropdown-content").classList.toggle("hidden"),
        e.querySelector(".dropdown-background").classList.toggle("hidden")
}
var OS = void 0;
navigator.userAgent.indexOf("Win") != -1 && (OS = "windows"),
    navigator.userAgent.indexOf("Mac") != -1 && (OS = "mac"),
    navigator.userAgent.indexOf("Linux") != -1 && (OS = "linux"),
    console.log(OS),
    document.querySelector("body").classList.add(OS)
